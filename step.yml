title: "BrowserStack App Automate - Downloader"
summary: A short summary of the step. Don't make it too long ;)
description: |-
  This is a Step template.
  Contains everything what's required for a valid Stepman managed step.

  A Step's description (and generally any `description` property)
  can be a [Markdown](https://en.wikipedia.org/wiki/Markdown) formatted text.

  To create your own Step:

  1. Create a new repository on GitHub
  2. Copy the files from this folder into your repository
  3. That's all, you can use it on your own machine
  4. Once you're happy with it you can share it with others.
website: https://github.com/...
source_code_url: https://github.com/...
support_url: https://github.com/.../issues
host_os_tags:
  - osx-10.10
project_type_tags:
  - android
type_tags:
  - utility
is_requires_admin_user: true
is_always_run: false
is_skippable: false
deps:
  brew:
    - name: curl
  apt_get:
    - name: curl
inputs:
  - browserstack_username:
    opts:
      title: BrowserStack username
      summary: Your username for browserstack
      description: |

      is_expand: true
      is_required: true
      is_sensitive: true
  - browserstack_access_key:
    opts:
      title: BrowserStack access key
      summary: Your username for browserstack
      description: |

      is_expand: true
      is_required: true
      is_sensitive: true
  - browserstack_build_id:
    opts:
      title: BrowserStack build id
      summary: build id for browserstack
      description: |

      is_expand: true
      is_required: true
